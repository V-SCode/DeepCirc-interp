# DeepCirc-interp — pip-installable dependencies
# Prefer environment.yml (conda) for a fully reproducible env.
# This file targets the figure-assembly + analysis path; back-end training
# additionally needs GPU torch and the upstream DeepCirc submodule installed
# via `pip install -e upstream/DeepCirc`.

# Core numerics
numpy>=1.26,<3
pandas>=2.0,<3
scipy>=1.11,<2

# I/O
h5py>=3.10,<4
pyyaml>=6.0,<7
pyarrow>=14

# Graphs
networkx>=3.1,<4

# Plotting (vector-first)
matplotlib>=3.8,<4
Pillow>=10.0
svgwrite>=1.4,<2
cairosvg>=2.7
scienceplots>=2.1

# ML (CPU-only; for figure rebuild this is only needed if you re-run interp scripts)
torch>=2.0
scikit-learn>=1.3,<2
