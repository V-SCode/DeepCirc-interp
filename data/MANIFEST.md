# Data manifest

This file documents every data dependency of DeepCirc-interp, where each
artifact comes from, and how reviewers obtain it. The layout matches what the
analysis + figure scripts expect after path rewrites in Phase 3.

## Three-tier strategy

| Tier | Hosting | Size | Purpose |
|---|---|---|---|
| **0 — In-repo** | This repository (`data/topology_g3/` + `data/interp_processed/`) | ~3 MB | Small CSV/JSON intermediates that feed the S10–S15 panel build scripts. Committed directly so `make figures` works after `git clone` alone. |
| **1 — Zenodo "figures" tier** | Zenodo deposit (DOI pending) — `figures__*` files | ~10 MB (superset of tier 0) | Same as tier 0 + a handful of larger CSVs used by `topology/scripts/2X*.py` re-runs. Fetched via `scripts/download_data.py --tier figures`. |
| **2 — Zenodo "full" tier** | Zenodo deposit (DOI pending) — `full__*` files | ~tens of GB | Trained MLP checkpoints, design-space predictions, raw exemplar HDF5 + checkpoints, PPO+GAT agent registries. Fetched via `scripts/download_data.py --tier full`. |

## Tier 0 — In-repo data (committed)

```
data/
├── topology_g3/
│   ├── best_perm_per_topology.csv          ~73 KB
│   ├── best_perm_summary.json              ~21 KB
│   ├── topology_graphs.json                ~116 KB     # feeds figS13 panel c
│   ├── size_tier_designs/
│   │   ├── dense_percentile_sweep.json     ~233 KB     # feeds figS10 panels b, c
│   │   ├── dense_percentile_sweep_3input.json ~234 KB
│   │   ├── size_tier_thresholds.json       ~4 KB
│   │   └── size_tier_design_counts.csv     ~29 KB
│   ├── motif_tier_analysis/
│   │   ├── structural_iso_by_size_circuit_k{3,4,5}*.csv          # feeds figS10 panel d
│   │   ├── typed_expansions_by_size_circuit_internal_only.csv    # feeds figS11
│   │   └── *.json (summary metadata)
│   ├── l2_top05/
│   │   ├── l2_enrichment.csv               ~999 KB     # feeds figS14 panels a, b, c
│   │   └── l2_summary.json
│   ├── panel_c_shapley/
│   │   └── shapley_per_design.json         ~28 KB      # feeds figS13 + figS15a
│   └── pareto/
│       ├── all_topology_fronts.csv         ~880 KB     # feeds figS12 panel b (3×3 NPN grid)
│       ├── knee_designs.csv                ~120 KB     # feeds figS13 panel c (max-circ + Pareto-knee cohorts)
│       ├── cross_target_portfolio.csv      ~13 KB
│       └── pareto_summary.json             ~2 KB
└── interp_processed/
    ├── shapley_taylor_sim_0x{2B,17,6D}.json   ~10 KB total   # feeds figS15b (primary)
    └── pairwise_0x{2B,17,6D}.json             ~10 KB total   # feeds figS15b (fallback)
```

## Tier 1 — Zenodo "figures" tier (in addition to Tier 0)

```
data/
└── topology_g3/
    ├── design_predictions/                  # per-topology MLP design-space predictions
    │   └── target=0x*/topology=*.parquet    # ~100 MB total
    └── pareto/                              # per-topology Pareto frontiers
        └── *.json                           # ~10 MB total
```

Use case: re-running `topology/scripts/14_l1_pareto_frontier.py`, `23_3_*`, `26_*`, `27_*` from scratch without retraining MLPs.

## Tier 2 — Zenodo "full" tier (in addition to Tiers 0 + 1)

```
data/
├── topology_g3/
│   ├── registries/                          # PPO+GAT shared registries per target
│   │   └── target=0x*/final_shared_registry.pkl   # ~100 MB per file × 20 ≈ 2 GB
│   ├── mlp_checkpoints/                     # trained Stage-2 MLPs (circuit + growth)
│   │   └── topology=*/mlp_{circuit,growth}.pt     # ~2 MB per checkpoint × 215 × 2 ≈ 860 MB
│   └── population.pkl + qc_tiers.pkl        # population manifests
└── exemplars/
    ├── 0x2B_design/
    │   ├── sampled_valid_permutations_5_gates_*.h5   # ~few hundred MB per exemplar
    │   ├── circuit_score_model.pt
    │   ├── toxicity_score_model.pt
    │   └── library_parts.csv
    ├── 0x17_design/...
    └── 0x6D_design/...
```

Use case: fully reproducing the back-end (PPO+GAT runs, MLP training, design-space scoring) from scratch.

## Provenance

| Artifact | Generated by | Inputs |
|---|---|---|
| `registries/target=*/final_shared_registry.pkl` | `topology/scripts/01_generate_topologies.py` + `slurm/p1_ppo.sbatch` | `configs/target_functions.yaml` + upstream DeepCirc training framework |
| `mlp_checkpoints/topology=*/mlp_*.pt` | `topology/scripts/04_train_mlps.py` + `slurm/p4_mlp_train.sbatch` | `population.pkl` + per-topology valid-permutation HDF5 samples |
| `design_predictions/target=*/*.parquet` | `topology/scripts/07_score_design_space.py` | `mlp_checkpoints/` + valid-permutation enumeration |
| `pareto/*.json` | `topology/scripts/14_l1_pareto_frontier.py` | `design_predictions/` |
| `topology_graphs.json` | `topology/scripts/22_extract_topology_graphs.py` | `population.pkl` |
| `size_tier_designs/*.json` | `topology/scripts/23_3_dense_percentile_sweep.py` | `population.pkl` + `qc_tiers.pkl` + `design_predictions/` |
| `motif_tier_analysis/*.csv` | `topology/scripts/26_l3_motif_size_aggregation.py` + `27_l3_motif_size_typed_expansion.py` | `population.pkl` + L2 enrichment outputs |
| `l2_top05/l2_enrichment.csv` | `topology/scripts/09_l2_graph_role.py` | `design_predictions/` |
| `panel_c_shapley/shapley_per_design.json` | `topology/scripts/29_panel_c_shapley.py` | curated 30-design table + `design_predictions/` |
| `shapley_taylor_sim_0x*.json` | `interp/scripts/04d_shapley_taylor_sim.py` | per-exemplar HDF5 samples + MLP checkpoints + simulator |
| `pairwise_0x*.json` | `interp/scripts/04b_pairwise_interactions.py` | per-exemplar HDF5 samples + MLP checkpoints |

## How to obtain the data

### Just rebuild the figures (laptop, minutes)

Tier 0 is already in the repository — no downloads needed:

```bash
make figures
```

### Re-run cross-topology analyses (laptop, ~hour with predictions)

Download Tier 1:

```bash
python scripts/download_data.py --tier figures --doi 10.XXXX/XXXXX
make analyses
make figures
```

### Re-run from scratch (HPC, days to weeks)

Tier 2 contains the artifacts your scripts will rebuild from upstream. Either
download them or regenerate from upstream DeepCirc + SLURM. See top-level
README "Path 2" section.

```bash
python scripts/download_data.py --tier full --doi 10.XXXX/XXXXX
# OR generate from scratch via:
make targets && make topologies && make population && make mlps && \
  make qc && make scoring && make analyses && make interp && make figures
```

## Notes

- All large artifacts (`.pkl` registries, `.pt` checkpoints, `.parquet`
  predictions, `.h5` exemplar samples) are gitignored — they live on Zenodo.
- Tier 0 in-repo data is verified to reproduce the published S10–S15 panels.
  If you find a discrepancy, file an issue with the exact panel + output diff.
